# Optional when the frontend is not served through the Vite /api proxy.
VITE_API_BASE_URL=/api/v1

# Validation mocks are isolated and clearly labelled. Keep disabled in production.
VITE_ENABLE_VALIDATION_MOCKS=false
